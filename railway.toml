[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --max-requests 1000 --max-requests-jitter 100"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
